# Backend Configuration (For Local or Render)
# Required for `node backend/index.js`
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
OPENAI_API_KEY=sk-proj-your-openai-api-key
# OPENAI_BASE_URL=https://api.openai.com/v1 (Optional: Leave empty for default OpenAI)
OPENAI_MODEL=gpt-4o
PORT=3000

# Frontend Configuration (Expo / Mobile App)
# Required for `npx expo start` or `eas build`
# 1. For Android Emulator: http://10.0.2.2:3000
# 2. For iOS/Web Local: http://localhost:3000
# 3. For Production/APK: https://your-app.onrender.com (Deployed Backend URL)
EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# This is used by the frontend to display which model is active (cosmetic only)
EXPO_PUBLIC_OPENAI_MODEL=gpt-4o